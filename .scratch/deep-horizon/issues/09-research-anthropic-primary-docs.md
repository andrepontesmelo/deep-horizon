# Research: confirm the prior-art finding against primary sources

Type: research
Status: open
Blocked by: —

## Question

The kickoff prior-art check found Claude Code `/goal`, Anthropic **Outcomes**,
**Dreaming** and **Routines** — but every citation was secondary press
(datasciencedojo, s5labs, substack). Direct searches against anthropic.com
returned nothing.

Confirm or correct, against **primary** sources only (anthropic.com,
docs.anthropic.com, Claude Code release notes/changelog, the Claude Code repo
or its bundled docs, `claude --help` on this machine if installed):

1. Does `/goal` exist, in which version, and is it session-scoped or
   persisted? Does it survive process restart?
2. Do **Outcomes** / **Dreaming** exist as named products, and are they
   Managed-Agents-only (i.e. not available to a local CLI harness)?
3. Is there ANY Anthropic primitive that persists a human-authored aim across
   sessions and is readable by other tools?

If any of these turns out to already be deep-horizon, that is the single most
valuable finding on this map — say so loudly.

## Anti-fabrication

Primary sources only. Quote the sentence and give the URL. If a primary source
cannot be reached, report that instead of falling back to press coverage.
