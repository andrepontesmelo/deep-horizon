# Research: Hermes + pi session-start injection surfaces

Type: research
Status: open
Blocked by: —

## Question

Same six-part question as ticket 01, but for **Hermes** and **pi**:

1. Hook/plugin/extension name and its configuration surface.
2. Where injected text lands and whether the model sees it.
3. New-session vs resumed-session discrimination.
4. Subagent exclusion.
5. Can it shell out to the `horizon` CLI and consume stdout?
6. Version the answer is true for.

Hermes is installed on this machine — prefer reading the live installation and
`https://claude-code.nousresearch.com/docs` over guessing. pi is at ~/.pi with
the pi-telegram extension already in use as a working example of pi's
extension surface.

## Anti-fabrication

Live evidence only. Name the file paths read. If pi has no session-start hook,
that is a valid and important answer — say so, and note the closest available
alternative.
